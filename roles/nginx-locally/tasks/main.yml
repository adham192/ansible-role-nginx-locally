#SPDX-License-Identifier: MIT-0
---
# tasks file for roles/nginx-locally

- name: Install nginx
  apt:
    name: nginx
    state: present
    update_cache: true

- name: Start nginx
  service: 
    name: nginx
    state: started
    enabled: true
  notify: Restart nginx
